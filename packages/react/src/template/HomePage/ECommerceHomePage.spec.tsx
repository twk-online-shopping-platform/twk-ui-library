import React from "react";
import { screen, render } from "@testing-library/react";
import Header from "../../Organisms/Header/Header";

describe("Test EcommerceHomePage", () => {
  it("should display home page with header, product cards, footer", async () => {
    //     const headerComp = <Header headerMenu={{ menuItems: [] }} />;
    //     render(<ECommerceHomePage header={headerComp} />);
  });
});
